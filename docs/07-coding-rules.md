Rules

Use TypeScript.

No any.

Business logic goes into services.

Controllers should be thin.

Repositories only access Prisma.

Never calculate financial data in React.

Never store wallet balance.

Never duplicate calculations.

Use reusable components.

Keep components small.